domain = "https://localhost:8080"
redis_uri = "redis://redis:6379"
tls_cert = "localhost+2.pem"
tls_key = "localhost+2-key.pem"
# Allow HTTP URLs and private IPs for federation requests (development only, default: false)
allow_http = false
